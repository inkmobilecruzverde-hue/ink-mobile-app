export const metadata = {
  title: "Ink-Mobile",
  description: "Gestión de reparaciones",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}